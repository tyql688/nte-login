# 复制为 .env 后按需修改；未列出的字段走 settings.py 默认值

# 监听
HOST=0.0.0.0
PORT=7861
LOG_LEVEL=INFO

# 与 NTEUID 后台 NTELoginSecret 一致；留空则不校验签名（启动会有 warning）
SHARED_SECRET=

# 单个登录会话存活秒数
SESSION_TTL_S=600

# HMAC 签名容忍时差秒数（防重放窗口）
SIG_TTL_S=300
